{
  "name": "exercise-2",
  "version": "0.2.0",
  "scripts": {
    "test": "jest --verbose",
    "test:debug": "node --inspect node_modules/.bin/jest --runInBand",
    "start": "jest --watch"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "jest": "^23.6.0"
  }
}
